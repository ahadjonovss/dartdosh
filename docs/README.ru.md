# DartDosh CLI – Руководство пользователя

**📖 Читать на других языках:**
[🇬🇧 English](../README.md) | [🇺🇿 O'zbekcha](README.uz.md) | [🇹🇷 Türkçe](README.tr.md)

---

`DartDosh` — это мощный инструмент командной строки для упрощения сборок Flutter с автоматическим управлением версиями, многоязычной поддержкой и умной обработкой выходных файлов.

## 🌍 Многоязычная поддержка

**DartDosh теперь поддерживает 4 языка!**

- 🇺🇿 **Узбекский** (uz) - стиль "Xo'jayiin" - По умолчанию
- 🇬🇧 **Английский** (en) - стиль "Boss"
- 🇷🇺 **Русский** (ru) - стиль "Босс"
- 🇹🇷 **Турецкий** (tr) - стиль "Patron" - **НОВИНКА!**

## 📺 Видео-урок

[![DartDosh Tutorial](https://img.youtube.com/vi/QpNVwk4SVZA/maxresdefault.jpg)](https://youtu.be/QpNVwk4SVZA?si=kV7kTtHcnZLfv1ZP)

Посмотрите полный урок на YouTube: [DartDosh - Flutter Build Automation](https://youtu.be/QpNVwk4SVZA?si=kV7kTtHcnZLfv1ZP)

---

```bash
dartdosh build <target> --<environment> [extra flags]
```

**Примеры:**

```bash
# С окружением (flavor builds) - автоматическое управление версиями
dartdosh build ipa --production --split
dartdosh build apk --development --other-flag
dartdosh build appbundle --staging

# Короткие флаги (удобно!)
dartdosh build apk -p              # production
dartdosh build apk -prod           # production
dartdosh build apk -d              # development
dartdosh build apk -dev            # development
dartdosh build apk -s              # staging

# Без окружения - обычная сборка Flutter (без управления версиями)
dartdosh build apk                 # flutter build apk
dartdosh build apk --release       # flutter build apk --release
dartdosh build ipa --split         # flutter build ipa --split-per-abi
```

---

## Требования

* Dart SDK ≥ 3.0
* Flutter SDK установлен
* Терминал (Mac/Linux/Windows)

---

## Установка

### Установка из pub.dev

```bash
dart pub global activate dartdosh
```

### Добавление в PATH (при необходимости)

**Mac/Linux (`.zshrc` или `.bashrc`):**

```bash
export PATH="$PATH:$HOME/.pub-cache/bin"
source ~/.zshrc
```

**Windows PowerShell:**

```powershell
$env:PATH += ";$env:USERPROFILE\.pub-cache\bin"
```

---

## Настройка

### Инициализация DartDosh

Сначала выполните эту команду в вашем Flutter проекте:

```bash
dartdosh init
```

Это создаст папку `dartdosh_config` с 2 файлами:

### 1. `build_config.json` (Общий для команды - отслеживается Git)

Команды сборки для вашей команды. Все используют одинаковые команды:

```json
{
  "apk": {
    "production": "flutter build apk --release --flavor production",
    "staging": "flutter build apk --release --flavor staging",
    "development": "flutter build apk --debug --flavor development"
  },
  "ipa": {
    "production": "flutter build ipa --release --flavor production",
    "staging": "flutter build ipa --release --flavor staging",
    "development": "flutter build ipa --debug --flavor development"
  },
  "appbundle": {
    "production": "flutter build appbundle --release --flavor production",
    "staging": "flutter build appbundle --release --flavor staging",
    "development": "flutter build appbundle --debug --flavor development"
  },
  "firebase_distribution": {
    "production": {
      "app_id": "1:123456789:android:prodabc123",
      "tester_groups": "production-testers,management"
    },
    "staging": {
      "app_id": "1:123456789:android:stagabc123",
      "tester_groups": "qa-team,staging-testers"
    },
    "development": {
      "app_id": "1:123456789:android:devabc123",
      "tester_groups": "developers,internal-testers"
    }
  }
}
```

### 2. `settings.json` (Персональный - игнорируется Git)

Ваши личные настройки. У каждого разработчика свои:

```json
{
  "language": "uz",
  "project_name": "my_app",
  "auto_increment_build_number": false,
  "output_path": "~/Desktop/dartdosh-builds",
  "ipa_upload": {
    "enabled": true,
    "apple_id": "developer@example.com",
    "app_specific_password": "abcd-efgh-ijkl-mnop"
  },
  "firebase_distribution": {
    "production": {
      "enabled": false
    },
    "staging": {
      "enabled": true
    },
    "development": {
      "enabled": true
    }
  }
}
```

**Зачем 2 файла?**

- `build_config.json` → Команда делится командами сборки (отслеживается Git)
- `settings.json` → Ваши личные настройки вроде Apple ID (игнорируется Git)
- Больше никаких конфликтов с учётными данными в команде!

**Параметры настроек:**

* `language`: Язык интерфейса
  - `uz` (узбекский), `en` (английский), `ru` (русский), `tr` (турецкий)
  - По умолчанию: `uz`

* `project_name`: Название проекта для организации сборок
  - По умолчанию: Читается из `pubspec.yaml`

* `auto_increment_build_number`: Автоматическое увеличение версии
  - `true`: Увеличивает номер сборки перед каждой сборкой
  - `false`: Сохраняет текущую версию (по умолчанию)

* `output_path`: Куда сохранять сборки
  - По умолчанию: `~/Desktop/dartdosh-builds`
  - Абсолютный или относительный путь

* `ipa_upload`: Автоматическая загрузка IPA в App Store
  - `enabled`: true/false
  - `apple_id`: Ваш Apple ID
  - `app_specific_password`: Получите с appleid.apple.com
  - Только для macOS

* `firebase_distribution`: Автоматическая загрузка APK в Firebase App Distribution (для каждого окружения)
  - Каждое окружение (production, staging, development) имеет:
    - `enabled`: true/false - Включить/выключить загрузку для этого окружения
  - App ID и группы тестировщиков настраиваются в `build_config.json`
  - Требуется установленный Firebase CLI

**Как получить пароль для приложения:**
1. Перейдите на https://appleid.apple.com
2. Безопасность → Создать пароль
3. Назовите его "DartDosh"
4. Скопируйте и вставьте в settings.json

**Как настроить Firebase App Distribution:**
1. Установите Firebase CLI: `npm install -g firebase-tools`
2. Войдите: `firebase login`
3. Получите App ID из Firebase Console → Project Settings
4. Добавьте группы тестировщиков в Firebase Console → App Distribution
5. Включите и настройте в `settings.json`

---

## Использование

```bash
dartdosh build <target> [--<environment>] [extra flags]
```

**Параметры:**

* `<target>`: Цель сборки
  - `apk` - Android APK
  - `ipa` - iOS IPA
  - `appbundle` или `aab` - Android App Bundle (поддерживаются обе команды)

* `<environment>`: Окружение сборки (ОПЦИОНАЛЬНО - поддерживается несколько вариантов флагов!)
  - **Production**: `--production`, `-p`, `-prod`
  - **Staging**: `--staging`, `-s`
  - **Development**: `--development`, `-d`, `-dev`
  - **Примечание**: Если окружение не указано, выполняется обычная сборка Flutter без управления версиями

* `[extra flags]`: Дополнительные флаги сборки Flutter
  - `--split` - Для APK сборок автоматически добавляет `--split-per-abi`
  - `--obfuscate` - Обфускация Dart кода
  - `--dart-define=KEY=VALUE` - Определение переменных окружения
  - Любые другие флаги сборки Flutter

---

## 📱 Краткое руководство по сборке iOS (IPA)

Сборка iOS приложений с DartDosh проста и включает автоматическую загрузку в App Store Connect.

### Предварительные требования

1. **macOS с Xcode** - Требуется для iOS сборок
2. **Действительный аккаунт Apple Developer** - Для дистрибутивных сборок
3. **Настройка подписи кода** - Сертификаты и профили подготовки настроены в Xcode

### Базовая сборка IPA

```bash
# Production сборка
dartdosh build ipa --production
dartdosh build ipa -p

# Staging сборка
dartdosh build ipa --staging
dartdosh build ipa -s

# С обфускацией
dartdosh build ipa -p --obfuscate

# С dart-define переменными
dartdosh build ipa -p --dart-define=API_URL=https://api.production.com
```

### Автоматическая загрузка в App Store Connect

DartDosh может автоматически загружать ваш IPA в App Store Connect после успешной сборки.

**1. Получите пароль для приложения**

1. Перейдите на [appleid.apple.com](https://appleid.apple.com)
2. Войдите с вашим Apple ID
3. В разделе **Безопасность** → **Пароли приложений** → **Создать пароль**
4. Назовите его "DartDosh" и скопируйте пароль (формат: `xxxx-xxxx-xxxx-xxxx`)

**2. Настройте в `build_config.json`**

```json
{
  "ipa_upload": {
    "enabled": true,
    "apple_id": "your-email@example.com",
    "app_specific_password": "xxxx-xxxx-xxxx-xxxx"
  }
}
```

**3. Соберите и загрузите**

```bash
dartdosh build ipa -p
```

**Вывод:**
```
[████████████████████████████████] 100% - [ipa - production] - Готово!
✅ ipa сборка успешно завершена, Босс!
✅ Сборка сохранена: ~/Desktop/dartdosh-builds/my_app/ipa/prod_1.0.0_100.ipa, Босс!

📤 Загрузка IPA в App Store Connect...
📊 Идёт загрузка...
✅ IPA успешно загружен в App Store Connect, Босс!

⏱️ Общее время: 180.5 секунд
```

### Выходные файлы

IPA файлы автоматически организуются:

```
~/Desktop/dartdosh-builds/
└── my_app/
    └── ipa/
        ├── prod_1.0.0_100.ipa
        ├── prod_1.0.0_101.ipa
        └── stg_1.0.0_50.ipa
```

**Формат именования файла:** `{env}_{version}_{buildNumber}.ipa`

### Важные замечания

- **Загрузка требует macOS** - Использует `xcrun altool` (встроен в Xcode)
- **Загрузка опциональна** - Установите `"enabled": false` для отключения
- **Безопасность** - Добавьте `build_config.json` в `.gitignore` для защиты учётных данных
- **Приложение должно существовать в App Store Connect** - Сначала создайте ваше приложение на [appstoreconnect.apple.com](https://appstoreconnect.apple.com)
- **Загрузка не отправляет на проверку** - Она только загружает сборку; вам всё ещё нужно отправить через App Store Connect

### Устранение неполадок

**Загрузка не удалась?**
- Проверьте ваш Apple ID и пароль приложения
- Убедитесь, что приложение существует в App Store Connect
- Проверьте интернет-соединение
- Убедитесь, что инструменты командной строки Xcode установлены: `xcode-select --install`

**Сборка не удалась?**
- Проверьте подписание кода в Xcode
- Убедитесь, что профили подготовки действительны
- Запустите `flutter doctor` для проверки настройки iOS

---

## Функции

### 🌍 Многоязычная поддержка
DartDosh поддерживает три языка для всех сообщений интерфейса и индикаторов прогресса:

* **Узбекский (uz)** - Язык по умолчанию с обращением "Xo'jayiin" (Босс)
* **Английский (en)** - Профессиональный английский интерфейс с обращением "Boss"
* **Русский (ru)** - Русский интерфейс с обращением "Босс"

**Установка языка:**
```json
{
  "language": "en"  // Установите в build_config.json
}
```

**Возможности языка:**
* Все сообщения логов переведены
* Этапы прогресс-бара локализованы
* Сообщения статуса сборки на выбранном языке
* Сообщения о пожертвованиях с культурным юмором
* Автоматический переход на английский для неподдерживаемых языков с предупреждением

**Пример на узбекском:**
```
📈 Yangi build number: 46 (oldingi: 45), Xo'jayiin!
[████████████████░░░░░░░░░░░░░░]  60% - [apk - production] - Bundle yaratilmoqda...
✅ apk build muvaffaqiyatli yakunlandi, Xo'jayiin!
```

**Пример на английском:**
```
📈 New build number: 46 (previous: 45), Boss!
[████████████████░░░░░░░░░░░░░░]  60% - [apk - production] - Creating bundle...
✅ apk build completed successfully, Boss!
```

### 🤖 Автоматическая конфигурация
Когда `build_config.json` не существует в вашем Flutter проекте, DartDosh:
1. **Создаст конфигурацию** с разумными значениями по умолчанию
2. **Откроет её в вашей IDE** автоматически для проверки
3. **Остановит выполнение** и попросит вас повторно запустить команду

Это гарантирует, что вы можете проверить и настроить конфигурацию перед первой сборкой.

**Пример рабочего процесса:**
```bash
# Первый запуск (конфигурация не существует)
dartdosh build apk --production
# Вывод: Конфигурация создана и открыта в IDE, пожалуйста, проверьте и запустите снова

# Второй запуск (конфигурация проверена)
dartdosh build apk --production
# Вывод: Обычная сборка продолжается
```

### 🔢 Автоматическое управление версиями (Опционально)

**При использовании флагов окружения** (flavor builds) и `auto_increment_build_number: true`, DartDosh автоматически:
1. Читает текущую версию из `pubspec.yaml`
2. Увеличивает номер сборки на 1
3. Обновляет `pubspec.yaml` с новым номером сборки

**Пример:**
```yaml
# Перед сборкой (с флагом окружения и включённым auto_increment)
version: 1.2.3+45

# После сборки
version: 1.2.3+46
```

**Примечания**:
- **По умолчанию**: `false` (автоинкремент отключён по умолчанию)
- Увеличение версии происходит только для flavor сборок (с флагом окружения)
- Включите, установив `auto_increment_build_number: true` в конфигурации
- Обычные сборки (`dartdosh build apk`) никогда не изменяют номера версий

### 📦 Умное именование файлов

**Для flavor сборок** (с флагами окружения), собранные файлы автоматически переименовываются по формату:
```
{shortEnv}_{version}_{buildNumber}.{extension}
```

**Короткие имена окружений:**
- `production` → `prod`
- `development` → `dev`
- `staging` → `stg`

**Примеры:**
* `prod_1.2.3_46.apk`
* `stg_2.0.0_12.ipa`
* `dev_1.5.0_78.aab`

Для разделённых APK:
* `prod_1.2.3_46_arm64-v8a.apk`
* `prod_1.2.3_46_armeabi-v7a.apk`
* `prod_1.2.3_46_x86_64.apk`

**Для обычных сборок** (без окружения), файлы переименовываются по формату:
```
{target}_{version}_{buildNumber}.{extension}
```

**Примеры:**
* `apk_1.2.3_46.apk`
* `ipa_2.0.0_12.ipa`
* `appbundle_1.5.0_78.aab`

Примечание: Обычные сборки не увеличивают версию, но всё равно переименовываются и перемещаются в output_path.

### 📁 Управление путём вывода

Если `output_path` указан в `build_config.json`:
* Собранные файлы **копируются** в указанную директорию с организованной структурой
* Файлы организованы по: `output_path/project_name/{apk|ipa|aab}/`
* Каждый тип сборки получает свою подпапку для лучшей организации
* Оригинальные файлы остаются в директории сборки
* Структура директорий создаётся автоматически

**Пример структуры:**
```
~/Desktop/dartdosh-builds/
└── my_app/
    ├── apk/
    │   ├── prod_1.0.0_100.apk
    │   └── dev_1.0.0_101.apk
    ├── ipa/
    │   └── prod_1.0.0_100.ipa
    └── aab/
        └── prod_1.0.0_100.aab
```

Без `output_path`:
* Файлы только **переименовываются** в директории сборки

---

## 🚀 Автоматическая загрузка IPA в App Store Connect

DartDosh может автоматически загружать ваши IPA файлы в App Store Connect после успешной сборки, используя официальный инструмент Apple Transporter.

### Предварительные требования

1. **macOS с установленным Xcode** - Требуется для `xcrun iTMSTransporter`
2. **Действительный аккаунт Apple Developer** - С созданным приложением в App Store Connect
3. **Действительные сертификаты и профили подготовки** - Сертификат iOS Distribution и профиль подготовки
4. **Пароль для приложения** - Сгенерированный из настроек Apple ID

### Шаг 1: Создание пароля для приложения

1. Перейдите на https://appleid.apple.com
2. Войдите с вашим Apple ID (тем, который используется для App Store Connect)
3. Перейдите в раздел **Безопасность**
4. В разделе **Пароли приложений** нажмите **Создать пароль**
5. Введите метку (например, "DartDosh CLI Tool")
6. Нажмите **Создать**
7. **Скопируйте сгенерированный пароль** (формат: `xxxx-xxxx-xxxx-xxxx`)
   - ⚠️ Сохраните этот пароль - вы не сможете просмотреть его снова!

### Шаг 2: Настройка build_config.json

Откройте ваш `build_config.json` и добавьте/обновите секцию `ipa_upload`:

```json
{
  "language": "uz",
  "project_name": "my_app",
  "auto_increment_build_number": false,
  "output_path": "~/Desktop/dartdosh-builds",
  "ipa_upload": {
    "enabled": true,                                    // ← Установите true
    "apple_id": "developer@example.com",                // ← Ваш Apple ID
    "app_specific_password": "abcd-efgh-ijkl-mnop"     // ← Вставьте сгенерированный пароль
  }
}
```

### Шаг 3: Сборка и загрузка

Просто соберите ваш IPA как обычно:

```bash
dartdosh build ipa --production
```

**Что происходит:**
1. ✅ Flutter собирает IPA
2. ✅ DartDosh переименовывает и перемещает файл
3. ✅ Автоматически загружает в App Store Connect
4. ✅ Показывает прогресс загрузки и результат

**Пример вывода:**
```
✅ ipa сборка успешно завершена, Босс!
📂 Файл сохранён: ~/Desktop/dartdosh-builds/my_app/ipa/prod_1.0.0_100.ipa

📤 Загрузка IPA в App Store Connect...
Файл: ~/Desktop/dartdosh-builds/my_app/ipa/prod_1.0.0_100.ipa
Apple ID: developer@example.com
✅ IPA успешно загружен в App Store Connect!
```

### Параметры конфигурации

| Поле | Тип | По умолчанию | Описание |
|-------|------|---------|-------------|
| `enabled` | boolean | `false` | Включить/выключить автоматическую загрузку |
| `apple_id` | string | `""` | Адрес электронной почты вашего Apple ID |
| `app_specific_password` | string | `""` | Пароль приложения из Apple ID |

### Устранение неполадок

**Ошибка: "IPA upload enabled but credentials not set!"**
- Убедитесь, что вы заполнили и `apple_id`, и `app_specific_password` в конфигурации

**Ошибка: "xcrun: error: unable to find utility"**
- Xcode не установлен или инструменты командной строки не настроены
- Установите Xcode из Mac App Store
- Выполните: `xcode-select --install`

**Ошибка: "Authentication failed"**
- Пароль приложения может быть неверным или истёкшим
- Создайте новый пароль приложения и обновите конфигурацию
- Убедитесь, что используете правильный Apple ID

**Ошибка: "Package upload failed"**
- Проверьте, что ваше приложение существует в App Store Connect
- Убедитесь, что сертификат iOS distribution действителен
- Убедитесь, что профиль подготовки соответствует сборке

**Загрузка медленная или зависла**
- Это нормально для больших IPA файлов (может занять несколько минут)
- Проверьте ваше интернет-соединение
- Инструмент покажет прогресс и будет ждать завершения

### Примечания по безопасности

⚠️ **Важные соображения безопасности:**

1. **Не коммитьте учётные данные в git**
   - Добавьте `build_config.json` в `.gitignore`
   - Никогда не пушьте файлы, содержащие ваш пароль приложения

2. **Пароли приложений безопаснее**
   - Они могут быть отозваны индивидуально
   - Не дают доступ к основному паролю Apple ID
   - Могут быть перегенерированы в случае компрометации

3. **Командная работа**
   - Каждый разработчик должен использовать свой собственный Apple ID и пароль
   - Или использовать общие учётные данные, хранящиеся в безопасном менеджере паролей
   - Рассмотрите использование секретов CI/CD для автоматизированных сборок

### Отключение автоматической загрузки

Чтобы отключить автоматическую загрузку, сохраняя ваши учётные данные:

```json
"ipa_upload": {
  "enabled": false,  // ← Просто установите false
  "apple_id": "developer@example.com",
  "app_specific_password": "abcd-efgh-ijkl-mnop"
}
```

---

## Примечания

### Флаги окружения
* **Полные флаги**: `--production`, `--staging`, `--development`
* **Короткие флаги**: `-p`, `-prod`, `-s`, `-d`, `-dev`
* Все варианты работают одинаково

### Языковая поддержка
* **Поддерживаемые**: `uz` (узбекский), `en` (английский), `ru` (русский), `tr` (турецкий)
* **По умолчанию**: Узбекский (`uz`)
* **Использование**: Используйте флаг `--language` или `-l`:
  ```bash
  dartdosh build apk -p --language tr    # Турецкий
  dartdosh build apk -p -l uz            # Узбекский
  dartdosh build ipa -s -l en            # Английский
  dartdosh build aab -d -l ru            # Русский
  ```
* **Или установите в конфигурации**: В `dartdosh_config/settings.json`:
  ```json
  {
    "language": "tr"
  }
  ```
* **Неподдерживаемый язык**: Автоматически переключается на английский с предупреждением:
  ```
  ⚠️  Warning: Language "fr" is not supported. Falling back to English.
     Supported languages: uz (Uzbek), en (English), ru (Russian), tr (Turkish)
  ```

### Поведение сборки
* Для APK сборок `--split` автоматически добавляет `--split-per-abi`
* Любые дополнительные флаги после базовой команды добавляются автоматически
* Номер сборки увеличивается **перед** началом сборки
* Все сообщения персонализированы ("Xo'jayiin" для узбекского, "Boss" для английского, "Босс" для русского)
* Отсутствующий `build_config.json` автоматически создаётся с настройками по умолчанию
* Прогресс-бар показывает этапы сборки в реальном времени на выбранном вами языке
